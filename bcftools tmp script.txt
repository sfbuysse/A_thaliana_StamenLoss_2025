bcftools filter -O z -o filtered.vcf.gz -i '%QUAL>30 || %DP>5 || ' [options] in.vcf.gz

for x in 0 0.25 0.50 0.70 0.75 0.80 0.85 0.95; do echo $x; awk 'BEGIN{getline;getline;for(x=1;x<=NF;x++){if($x=="variants_effect_missense_variant"){A=x};if($x=="variants_effect_synonymous_variant"){B=x}};L=0;M=0} {L=L+$A;M=M+$B} END{print "Missense: " L " Synon: " M }' ./MaxMiss"$x"/snpEff_genes.txt; done
